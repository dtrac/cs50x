#include <stdio.h>

int main(void) 
{
    string world = get_string("world");
   // printf() displays the string inside quotation
    printf("Hello, %s", world);
   return 0;
}


get input (number) 
input validation (+ve number between)

number is number of lines
hashes is number of hashes (1)

for number {
    print  hash
    

    
}